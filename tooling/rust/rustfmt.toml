# rustfmt configuration for Rust projects bootstrapped from the AI Engineering System.
#
# Keep this file small. rustfmt's defaults are good; only override when there is
# a deliberate project-wide reason.

edition = "2024"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
