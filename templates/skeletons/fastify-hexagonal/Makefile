.PHONY: setup run test lint fmt

setup:
	npm install

run:
	npm run dev

test:
	npm test

lint:
	# Phase 3 tooling will replace this with eslint when adopted.
	npx tsc --noEmit

fmt:
	# Phase 3 tooling adds prettier.
