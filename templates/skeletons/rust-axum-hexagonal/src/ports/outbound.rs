// Define outbound port traits here when the service grows external dependencies
// (databases, message brokers, third-party APIs). Each trait describes what the
// core needs from the outside world without committing to a concrete adapter.
