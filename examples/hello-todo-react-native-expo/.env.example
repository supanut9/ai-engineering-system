# Environment variables for hello-todo-react-native-expo
#
# Expo exposes variables prefixed with EXPO_PUBLIC_ to the client bundle.
# Example:
#   EXPO_PUBLIC_API_URL=https://api.example.com
#
# This app is intentionally local-only (no backend), so no variables are
# required. Add any future public configuration here.
