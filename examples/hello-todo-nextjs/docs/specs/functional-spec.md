# functional specification — hello-todo-nextjs

Behavioral contract for each endpoint and the home page. Supplements the acceptance
criteria in `../requirements/acceptance-criteria.md`. Implementation must match this
exactly.

---

## common behavior

### content-type

All write requests (`POST`, `PATCH`) must include `Content-Type: application/json`.
Requests with a body that is not valid JSON return:
- status 400
- body `{"error":{"code":"validation_error","message":"..."}}`

### error envelope

Every non-2xx API response body is:
```json
{"error":{"code":"<code>","message":"<human readable>"}}
```
Valid `code` values: `not_found`, `validation_error`, `internal`.

### id format

Ids are 32-char hex strings generated by `crypto.randomBytes(16).toString('hex')`.
Callers must treat ids as opaque strings.

---

## POST /api/todos

### input

```json
{
  "title": "string, required, non-empty after trim, max 200 chars",
  "due_at": "string, optional, ISO8601 format"
}
```

### validation rules

- `title` must be present and non-empty after leading/trailing whitespace is trimmed.
  Whitespace-only strings (e.g., `"   "`) are rejected.
- `title` trimmed length must be ≤ 200 characters.
- `due_at`, if provided, must parse as a valid ISO8601 timestamp. An invalid format
  returns 400 with `validation_error`.
- Extra JSON fields not listed above are silently ignored by the service layer.

### success behavior

- server generates a unique opaque id
- `created_at` and `updated_at` are set to the current server UTC time as ISO8601
- `completed` defaults to `false`
- `due_at` is stored as-is if valid, or `null` if omitted
- returns 201 with the full todo object

### error behavior

| condition | status | code |
|---|---|---|
| missing or empty title | 400 | `validation_error` |
| title exceeds 200 chars | 400 | `validation_error` |
| malformed JSON body | 400 | `validation_error` |
| invalid `due_at` format | 400 | `validation_error` |

---

## GET /api/todos

### behavior

- returns all todos in the in-memory store
- returns empty `{"items":[]}` when no todos exist
- order preserves insertion order (Map iteration + separate order array)
- no pagination, filtering, or sorting in v0.1.0

### success behavior

- status 200
- body `{"items": [<Todo>, ...]}`

---

## GET /api/todos/:id

### behavior

- looks up the todo by exact id match
- id is taken from the URL dynamic segment `[id]`

### success behavior

- status 200, full todo object

### error behavior

| condition | status | code |
|---|---|---|
| id not found in store | 404 | `not_found` |

---

## PATCH /api/todos/:id

### input

```json
{
  "title":     "string, optional",
  "due_at":    "string (ISO8601) or null, optional",
  "completed": "bool, optional"
}
```

### behavior

- only fields present in the request body are updated (partial update semantics)
- fields absent from the body are left unchanged
- `id`, `created_at` are immutable and ignored if supplied
- `updated_at` is set to the current server UTC time on any successful update
- sending `due_at: null` explicitly clears the due date

### validation rules

- if `title` is supplied, same rules apply as POST: non-empty after trim, ≤ 200 chars
- if `due_at` is supplied as a string, it must be valid ISO8601
- sending an empty body `{}` is accepted and returns the todo unchanged (idempotent)

### success behavior

- status 200, full updated todo object

### error behavior

| condition | status | code |
|---|---|---|
| id not found | 404 | `not_found` |
| empty title supplied | 400 | `validation_error` |
| title exceeds 200 chars | 400 | `validation_error` |
| malformed JSON | 400 | `validation_error` |
| invalid `due_at` format | 400 | `validation_error` |

---

## DELETE /api/todos/:id

### behavior

- removes the todo with the given id from the store
- a second DELETE on the same id returns 404

### success behavior

- status 204, empty body

### error behavior

| condition | status | code |
|---|---|---|
| id not found | 404 | `not_found` |

---

## GET /healthz

### behavior

- does not check storage health (in-memory; always available while process is running)
- returns immediately; no dependencies to probe

### success behavior

- status 200
- body `{"status":"ok"}`

---

## GET / (home page)

### behavior

- the `app/page.tsx` server component calls `todosService.findAll()` directly (no HTTP
  round-trip)
- renders the todo list as HTML server-side
- if the store is empty, renders a "No todos yet" message in place of the list

### success behavior

- status 200, Content-Type: text/html
- HTML contains each todo's `title`
- no JavaScript errors on load

---

## edge cases summary

| scenario | expected behavior |
|---|---|
| whitespace-only title on create | 400 validation_error |
| title of exactly 200 chars | 201 accepted |
| title of 201 chars | 400 validation_error |
| `due_at` as empty string | 400 validation_error |
| PATCH with `{}` empty body | 200, todo unchanged |
| PATCH `due_at: null` | 200, `due_at` cleared to null |
| unknown id in any mutating endpoint | 404 not_found |
| completely unknown API route | 404 (Next.js default) |
