.PHONY: setup run test lint fmt build

setup:
	npm install --no-audit --no-fund

run:
	npm run dev

test:
	npm test

lint:
	npm run lint

fmt:
	# format: add prettier in Phase 3 tooling
	@echo "fmt: configure prettier in Phase 3"

build:
	npm run build
